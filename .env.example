# This file is an example. Copy it to .env and fill in your actual configuration values.
# The .env file should be added to .gitignore and NOT committed to version control.

# SAP HANA Connection Details
HANA_ADDRESS=your_hana_instance_address
HANA_PORT=your_hana_instance_port
HANA_USER=your_hana_username
HANA_PASSWORD=your_hana_password

# Application Secret Key
# Used for session signing and other cryptographic operations.
# Generate a strong random key (e.g., using os.urandom(24).hex() in Python)
APP_SECRET_KEY=your_strong_random_secret_key

# Flask specific (if using Flask directly, Panel might have its own config)
# FLASK_SECRET_KEY=your_flask_secret_key
# FLASK_ENV=development # or production
# FLASK_DEBUG=True # or False for production
